# Carpeta de imágenes

Coloca aquí tus archivos SVG e imágenes.

Ejemplo de uso en componentes:

```tsx
import slotMachine from "../images/slot-machine.svg"

<img src={slotMachine} alt="Slot machine" />
```

